# requirements.txt
# Python 3.11 — CPU only, no CUDA
#
# IMPORTANT: install with the CPU-only PyTorch index to avoid
# downloading the GPU torch variant (~2 GB):
#
#   pip install torch==2.3.1 --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
#
# up.sh handles this automatically.

# ---------------------------------------------------------------------------
# API
# ---------------------------------------------------------------------------
fastapi==0.111.1
uvicorn[standard]==0.30.3
pydantic==2.7.4
slowapi==0.1.9
python-multipart==0.0.9          # required by FastAPI for form data

# ---------------------------------------------------------------------------
# Search — keyword
# ---------------------------------------------------------------------------
rank-bm25==0.2.2

# ---------------------------------------------------------------------------
# Search — vector
# ---------------------------------------------------------------------------
sentence-transformers==3.0.1
faiss-cpu==1.8.0

# torch is a transitive dependency of sentence-transformers.
# Pin the CPU build explicitly so pip does not pull a CUDA wheel.
torch==2.3.1
tokenizers==0.19.1
transformers==4.42.4
huggingface-hub==0.23.4

# ---------------------------------------------------------------------------
# Data
# ---------------------------------------------------------------------------
wikipedia-api==0.7.1
tqdm==4.66.4
numpy==1.26.4

# ---------------------------------------------------------------------------
# Dashboard
# ---------------------------------------------------------------------------
streamlit==1.35.0
plotly==5.22.0
requests==2.32.3

# ---------------------------------------------------------------------------
# Testing
# ---------------------------------------------------------------------------
pytest==8.2.2
pytest-asyncio==0.23.7
httpx==0.27.1

# ---------------------------------------------------------------------------
# Utils
# ---------------------------------------------------------------------------
python-dotenv==1.0.1